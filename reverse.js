for(i=10; i>0; i--){
    console.log(i);
}
// when you want to loop reverse in an array
let names = ["jen", "pk", "shap", "podda", "jero"];
for (i = names.length; i>=0; i--) {
  let name = names[i];
  console.log(name);
}